import { BrowserRouter, Switch, Route } from 'react-router-dom';

const Routes = () => {
    return (
        <BrowserRouter>
            <div>
                <Route path='/' component={} />
            </div>
        </BrowserRouter>
    )
}

export default Routes;